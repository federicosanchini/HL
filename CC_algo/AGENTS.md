# AGENTS.md

## Project Context (well be maintained)

This project aims at training an algorithm that sends prediction to CrowdCent (CC), as part of their challenge.
These predictions are the ranking of how cryptos will perform against each other.

The Dataset is a parquet file, whos content has yet to be explored.

## Colab use

The code will ultimately be uploaded in drive and used in colab.
Hence, the structure of codebase will be:

- python source scripts in ...src/
- main notebook file orchestrating the execution, that will run on colab.
